[package]
name = "private-search"
version = "0.1.0"
edition = "2024"

[dependencies]
rocket = { version = "0.5.1", features = ["json"] }
rocket_dyn_templates = { version = "0.2.0", features = ["handlebars"] }
private-search-engines = { path = "../engines" }
log = "0.4"
