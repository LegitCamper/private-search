/target
cache.db
cache.db-journal
cache.db-wal
cache.db-shm
data/
/main
node_modules/
# Engine HTML fixtures are committed deliberately so CI can detect upstream
# markup changes (e.g. a redesigned results page) without hitting the network.
