[package]
name = "search-engines"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.12.24", features = ["gzip", "brotli"] }
scraper = "0.24.0"
serde = { version = "1.0.228", features = ["derive"] }
percent-encoding = "2.3.2"
serde_json = "1.0"
async-trait = "0.1.89"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread", "net", "io-util"] }
